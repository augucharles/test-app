import { Lucid, PaymentKeyHash, UTxO } from "lucid-cardano";
declare type ScriptAssets = {
    address: string;
    nativeScript: {
        pkh: string;
        unlockTime: number;
    };
    assets: {
        currencySymbol: string;
        tokenName: string;
    }[];
};
export declare type ToClaim = {
    [key: string]: {
        nativeScript: {
            pkh: string;
            unlockTime: number;
        };
        asset: {
            currencySymbol: string;
            tokenName: string;
        };
    }[];
};
export declare const deduplicateUtxosReducer: (acc: UTxO[], cur: {
    utxos: UTxO[];
    nativeScript: any;
}) => UTxO[];
export declare const claimChecks: (lucid: Lucid) => (pkh: PaymentKeyHash, unlockTime: number, assets: {
    currencySymbol: string;
    tokenName: string;
}[]) => ((u: UTxO) => boolean)[];
export declare const buildTimelockedNativeScript: (slot: number, pkh: string) => import("lucid-cardano/custom_modules/cardano-multiplatform-lib-browser").NativeScript;
export declare const groupByScript: (toClaim: ToClaim) => ScriptAssets[];
export {};
